INCLUDE_DIRECTORIES(
	${CMAKE_SOURCE_DIR}/include 
	)


AUX_SOURCE_DIRECTORY(. CURRENT_SRC_LIST)

#AUX_SOURCE_DIRECTORY(./sum SUM_SRC_LIST)

set(EXECUTABLE_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/bin)

ADD_EXECUTABLE(test ${MAIN_SRC_LIST} ${SUM_SRC_LIST})
